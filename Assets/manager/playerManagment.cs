using UnityEngine;

public class playerManagment : MonoBehaviour
{
    public Color[] playerColors = {Color.green, Color.blue, Color.red, Color.yellow, Color.magenta, Color.cyan, Color.white, Color.black, Color.gray};
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
    
}
